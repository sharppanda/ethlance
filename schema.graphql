type Job @entity {
  id: ID!
  title: String!
  description: String!
  jobStatus: JobStatus!
}


enum JobStatus {
  Hiring
  HiringDone
  Blocked
  Draft
  Refunding
  Refunded
}

type DbMapping @entity {
  id: ID!
  jobId: ID!
  propertyType: String!
}